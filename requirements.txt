# Flask and web dependencies
flask
gunicorn
flask-login
bcrypt
python-dotenv
pymongo

# LangChain and AI dependencies
langchain==0.2.16
langchain-community==0.2.16
langchain-text-splitters==0.2.4
langchain-groq==0.1.9
langchain-huggingface

# Vector database and document processing
faiss-cpu
pypdf

# Note: PDFs stored in MongoDB (cloud storage)
# Vectors stored in FAISS (in-memory for fast search)
# Using HuggingFace API for embeddings (no local models downloaded)